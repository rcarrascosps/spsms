# docker box definition
box:
  id: maven:latest
  ports:
    - "8080"

# defining build pipeline
build:
  steps:
    - script:
      name: Maven Package
      code: mvn package
    - script:
      name: Confirm Maven Package
      code: echo "Paquete creado" && ls -ltr ./target/*